import {
    MenuCardContainer,
    StyledMenuSection,
} from "../components/styles/MenuCard.styled";

function MenuCard() {
    return (
        <MenuCardContainer>
            {/* <StyledMenuSection> */}
            <h1>Hello</h1>
            {/* </StyledMenuSection> */}
        </MenuCardContainer>
    );
}

export default MenuCard;
